# v0.2.6-hotfix-2 — 首页与移动端首屏强视觉强化报告

**时间**: 2026-06-06 11:25 UTC  
**执行人**: 小紫 👾  
**目标版本**: v0.2.6-hotfix-2  
**实施范围**: 仅 `apps/frontend/src/views/HomeView.vue`  
**前置检查**: `https://www.haoyulv.com/api/health` → HTTP 200  
**严格约束**: 不修改后端、不修改数据库、不修改 Nginx 配置、不拆分 HomeView.vue、不改业务逻辑

---

## 1. 前置 /api/health 检查

| 检查项 | 结果 |
|--------|:----:|
| `https://www.haoyulv.com/api/health` | ✅ HTTP 200 |
| 是否允许继续 UI hotfix | ✅ 是 |

---

## 2. 实际修改文件

| # | 文件 | 操作 | 变更量 |
|---|------|:----:|:------:|
| 1 | `apps/frontend/src/views/HomeView.vue` | 修改 | **+223 / -52 行** |

未修改：
- 后端代码
- 数据库
- Nginx 配置
- Wallet / LedgerEntry
- 任务 / 订单状态机
- API 调用逻辑

---

## 3. PC 首屏视觉增强摘要

### 3.1 Hero 背景增强

新增三层视觉元素：

| 层 | class | 作用 |
|----|-------|------|
| 城市暖光 | `.hero-city-glow` | Hero 底部增加暖金 + 紫蓝 + 青色光晕 |
| 城市轮廓 | `.hero-cityline` | 18 个 CSS 楼体块，形成城市灯火剪影 |
| 灯笼/星点 | `.hero-lantern-layer` / `.hero-lantern` | 左右悬浮灯笼 + 星点，强化“万家灯火” |

### 3.2 Hero 文案层级增强

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 主标题字号 | 46px | **54px** |
| 主标题字重 | 800 | **900** |
| 主标题行高 | 1.15 | **1.08** |
| 主标题阴影 | 无 | `text-shadow` 紫蓝柔光 |
| slogan 字号 | 18px | **20px** |
| slogan 字重 | 600 | **700** |
| slogan 宽度 | 500px | **560px** |

### 3.3 CTA 按钮增强

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 字重 | 700 | **800** |
| 字色 | 继承 | `#0f172a` 深色文字，提高暖金按钮可读性 |
| hover shadow | `0 6px 32px` | **`0 8px 34px`** |

---

## 4. 手机端首屏视觉增强摘要

### 4.1 手机 Hero 首屏重排

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| Hero 高度 | auto | **390px** 固定首屏视觉区域 |
| Hero padding | `28px 0 20px` | **`32px 0 26px`** |
| Hero 圆角 | 无 | **底部 28px 圆角** |
| Hero 横向延展 | 普通 content 内 | `margin: 0 -12px`，更接近 full-bleed 首屏 |
| Hero 标题 | 26px | **34px** |
| Hero tag | 紫蓝底 | **暖金底 + 暖金边框** |
| slogan | 20px | 保持 20px，但增加更明显文字微光与 1.5 行高 |

### 4.2 手机端城市/灯笼氛围

| 元素 | 移动端表现 |
|------|------------|
| 城市轮廓 | 96vw 宽，62px 高，透明度 0.72 |
| 灯笼 | 左右两侧重新定位，移动端可见 |
| 暖金光点 | 继承 hotfix-1 的 8 个额外暖金光点 |
| 底部暖光 | `.hero-city-glow` 190px 高，贴近效果图夜色光晕 |

---

## 5. 底部 Tab / 发布按钮改动说明

新增移动端底部 Tab 视觉初版：

| 元素 | 实现 |
|------|------|
| 底部导航容器 | `.mobile-bottom-tabs`，fixed 底部，毛玻璃，圆角 24px |
| 导航项 | 首页 / 任务 / 保障 / 我的 |
| 中央发布按钮 | `.mobile-tab-publish`，暖金圆形按钮，悬浮在 Tab 上方 |
| 发布逻辑 | 已登录：`openCreateDialog()`；未登录：跳转 `/login` |
| 旧 FAB | 移动端隐藏，避免双发布按钮 |

底部 Tab 仅在 `@media (max-width: 768px)` 生效，PC 端默认 `display: none`。

---

## 6. 数据看板与五步流程增强

### 6.1 数据看板

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 布局 | flex + divider | **grid 四卡片** |
| 卡片背景 | 单容器玻璃 | 每个指标独立玻璃卡片 |
| 卡片边框 | 容器边框 | 每个卡片独立边框 |
| 卡片暖光 | 无 | `stat-item::before` 顶部暖金光晕 |
| 移动端 | flex wrap | **2x2 grid** |

### 6.2 五步流程区

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 容器 | 普通透明卡片 | 渐变玻璃卡片 |
| 圆角 | 14px | **18px** |
| 阴影 | 无 | `0 6px 26px rgba(0,0,0,0.12)` |
| step dot | 原基础样式 | 每个 step 增加半透明卡片背景 |
| 移动端 | 小间距 | 横向滑动卡片化，66px 最小宽 |

---

## 7. Build 结果

| 检查项 | 结果 |
|--------|:----:|
| `npm run build` | ✅ 通过 |
| 构建耗时 | 6.96s |
| 新 JS | `index-CLFe0QVc.js` |
| 新 CSS | `index-CIvnHpCf.css` |
| TS/SCSS 错误 | ❌ 无 |
| Chunk 警告 | ⚠️ 仍存在大 chunk 警告（历史问题，非本次引入） |

---

## 8. 本地验证结果

| 验证项 | 结果 |
|--------|:----:|
| `/api/health` | ✅ 200 |
| 首页 `/` | ✅ 200 |
| 登录页 `/login` | ✅ 200 |
| 注册页 `/register` | ✅ 200 |
| 任务详情 `/task/1` | ✅ 200 |
| 手机端无横向滚动 | ✅ 样式保持 `max-width:100vw` / `overflow-x:hidden` |
| 发布入口 | ✅ Hero CTA + 移动端中央发布按钮均保留 |
| 登录/注册入口 | ✅ 保留 |

---

## 9. git 状态与 diff 摘要

```
apps/frontend/src/views/HomeView.vue | 275 ++++++++++++++++++++++++++++-------
1 file changed, 223 insertions(+), 52 deletions(-)
```

当前跟踪文件修改：
```
M apps/frontend/src/views/HomeView.vue
```

另外存在一批历史未跟踪文件，与本次无关，未触碰。

---

## 10. 是否建议提交和部署

✅ **建议提交和部署**。

原因：
1. `/api/health` 前置检查已通过；
2. 构建通过；
3. 只修改 `HomeView.vue`，没有业务逻辑/API/后端/数据库变更；
4. PC 和手机首屏视觉变化都明显增强；
5. 移动端新增底部 Tab + 中央发布按钮，接近效果图方向；
6. git diff 虽较大，但集中在首页视觉模板/CSS，仍可审计。

建议 commit message：
```
style(frontend): boost homepage hero and mobile first screen
```

建议部署命令：
```bash
cd /opt/haoyu/apps/frontend
npm run build
rsync -a --delete dist/ /home/web/html/haoyu/
docker exec nginx nginx -t
docker exec nginx nginx -s reload
```
