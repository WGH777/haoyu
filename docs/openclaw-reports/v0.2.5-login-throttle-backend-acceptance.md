# v0.2.5 登录限流后端阶段验收报告

**验收时间**: 2026-06-05 11:38 UTC  
**后端阶段 commit**: `a25a286` → `444e88e`（5 commits）  
**前端/Nginx**: 未参与

---

## 验收清单

| # | 验收项 | 结果 | 详情 |
|---|--------|:----:|------|
| 1 | `npm run build` | ✅ | `nest build` 零错误 |
| 2 | PM2 haoyu-backend online | ✅ | pid=3566154, uptime 2s（已重启，内存清零） |
| 3 | `GET /api/health` → 200 | ✅ | `{"status":"ok","timestamp":...,"time":"..."}` |
| 4 | 正确账号登录 → 200 | ✅ | admin@haoyulv.com → 200 + tokens |
| 5 | 错误密码第 6 次 → 429 | ✅ | `401 401 401 401 401 429`（5 次失败后 email 限流） |
| 6 | 成功登录后 email + IP 清除 | ✅ | admin 成功登录后，再次错误密码 → 401（非 429） |
| 7 | 同一 IP 多次成功不掉 429 | ✅ | 3 轮成功→失败→失败：全部 401，无误伤 |
| 8 | 429 不暴露账号存在性 | ✅ | message: `登录请求过于频繁，请稍后再试`（相同） |
| 9 | 日志无密码/token | ✅ | 仅 `[LoginThrottleGuard] Email acc*** login failures exceeded: 5/5` |
| 10 | www/admin/api 基础可用 | ✅ | www/admin 首页 200, /api/health 反代 200 |
| 11 | git status 干净 | ✅ | 无 dirty tracked 文件 |
| 12 | 是否建议进入 Nginx phase | ✅ | 后端保护层就绪，建议进入 Nginx limit_req 设计复核 |

---

## 验证时序（完整流程）

```
PM2 restart（内存清零）
  ↓
① /api/health                                          → ✅ 200
  ↓
② 正确登录 admin@haoyulv.com                            → ✅ 200 + tokens
  ↓
③ 错误密码 acceptance@fail.com ×6                       → ✅ 401 401 401 401 401 429
  ↓
④ admin@haoyulv.com 正确登录（清理 email + IP）           → ✅ 200
  ↓
⑤ admin@haoyulv.com 错误密码（email 已清，不触发 429）    → ✅ 401（正确行为）
  ↓
⑥ admin@haoyulv.com 成功失败循环 ×3（IP 每次被清）        → ✅ 均 401（无累积误伤）
  ↓
⑦ 429 JSON 结构 + 日志安全验证                           → ✅ 通过
  ↓
⑧ www / admin / api 反代                                 → ✅ 全部 200
```

---

## 后端限流最终策略（验收通过）

| 维度 | 窗口 | 上限 | 清除条件 |
|------|:----:|:----:|---------|
| **IP 请求数** | 15 分钟 | 10 次 | 成功登录后立即清除（`clearIp`） |
| **Email 失败数** | 15 分钟 | 5 次 | 成功登录后立即清除（`clearEmailFails`） |
| **过期间清理** | 60 秒周期 | — | 自动删除过期记录 |
| **进程重启** | — | — | 内存清零，自动解封 |

---

## 后端阶段 commits（5 个）

```
444e88e docs: add clearIp login throttle fix report
669a865 fix(backend): clearIp on successful login to prevent false positive throttling
f98c555 docs: add login throttle false positive review
3adc81f docs: add v0.2.5 login throttle implementation report
a25a286 feat(backend): add LoginThrottleGuard for /api/auth/login rate limiting
```

---

## 建议下一步

| 优先级 | 任务 | 说明 |
|--------|------|------|
| **P0** | ✅ 后端限流已就绪 | 验收通过，可进入 Nginx phase |
| **P1** | 📋 Nginx limit_req 设计复核 | 配置 `limit_req_zone` + `admin.haoyulv.com.conf` |
| **P2** | 📋 审计日志 LOGIN_FAILED 扩展 | `AdminAuditService` 类型扩展 + 调用 |
| **P3** | 📋 fail2ban 规则（可选） | 评估是否需要 |
