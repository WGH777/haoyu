# v0.2.6 Phase 4 — 任务详情页按钮层级与移动端阅读体验优化报告

**时间**: 2026-06-06 10:30 UTC  
**执行人**: 小紫 👾  
**目标版本**: v0.2.6  
**实施范围**: 仅 `apps/frontend/src/views/task/TaskDetail.vue`  
**设计依据**: `docs/design/v0.2.6-frontend-visual-upgrade-plan.md`  
**Phase 0-3 commit**: `fe68522` `c84dff9` `40ba820` `7808a8f`  
**严格约束**: 不拆分组件、不改业务逻辑、不改 API/后端/数据库

---

## 1. 实际修改文件

| # | 文件 | 操作 | 变更量 |
|---|------|:----:|:------:|
| 1 | `apps/frontend/src/views/task/TaskDetail.vue` | 修改 | **+49 / -26 行** |

仅修改 1 个文件，其他文件未动。

---

## 2. 任务详情页视觉改动摘要

### 2.1 Hero 头部暖金化

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 背景渐变 | `rgba(124,92,255,0.08) → rgba(246,183,60,0.04)` | `rgba(99,102,241,0.08) → rgba(251,191,36,0.04)` (品牌色+暖金) |
| 边框色 | `rgba(255,255,255,0.06)` | `rgba(148,163,184,0.1)` (融入全局 token) |
| 顶部 padding | `24px 28px` | `20px 28px` (略紧凑) |
| 返回按钮 hover | `#f6b73c` (旧 amber) | `#fbbf24` (暖金统一) |
| 价格文字 `price-value` | `color: #f6b73c` 纯色 | `linear-gradient(#fcd34d, #f59e0b)` 暖金渐变 |
| 价格背景 `hero-price` | `rgba(246,183,60,...)` amber | `rgba(251,191,36,...)` gold 统一色调 |

### 2.2 全局卡片统一

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 卡片背景 (detail-card) | `rgba(255,255,255,0.035)` | `rgba(17,24,39,0.55)` + `backdrop-filter: blur(8px)` |
| 卡片边框 | `rgba(255,255,255,0.07)` | `rgba(148,163,184,0.1)` (对齐 token) |
| aside-card 背景 | `rgba(255,255,255,0.035)` | `rgba(17,24,39,0.55)` + 毛玻璃 |
| aside-card 边框 | `rgba(255,255,255,0.07)` | `rgba(148,163,184,0.1)` |

所有卡片统一为 hy-card 风格的玻璃拟态，与首页风格一致。

### 2.3 赏金卡片暖金

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| escrow-number | `color: #f6b73c` | `linear-gradient(#fcd34d, #f59e0b)` 渐变 |
| escrow-tip 背景 | `rgba(124,92,255,0.06)` | `rgba(99,102,241,0.06)` (品牌紫蓝统一) |
| escrow-unit | `rgba(246,183,60,0.65)` | `rgba(251,191,36,0.6)` |

---

## 3. 按钮层级改动摘要

### 3.1 主按钮（接单/提交成果等）

```css
.mobile-primary-action {
  min-height: 44px;      /* 46→44 略减 */
  border-radius: 14px;
  font-weight: 700;
  font-size: 15px;       /* 新增 */
  box-shadow: 0 4px 16px rgba(99, 102, 241, 0.2);  /* 新增轻微发光 */
  width: 100%;
}
```

### 3.2 次按钮（通过/驳回）

```css
.mobile-secondary-action {
  min-height: 40px;      /* 42→40 略减 */
  border-radius: 12px;
  font-weight: 600;
  font-size: 14px;       /* 新增显式字体 */
}
```

### 3.3 三级按钮（争议/编辑/删除等）

```css
.mobile-small-action {
  min-height: 34px;
  border-radius: 10px;
  font-size: 13px;
  font-weight: 600;      /* 新增 */
}
```

### 3.4 操作按钮行

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| action-btn-row gap | `12px` | `10px` |
| 通过按钮阴影 | 无 | `0 2px 12px rgba(16,185,129,0.15)` |
| 驳回按钮阴影 | 无 | `0 2px 12px rgba(239,68,68,0.12)` |

### 3.5 视觉层级对比

```
PC 端:
 ┌─────────────────────────────┐
 │   [🚀 立即接单] ← 主按钮    │  44px, 圆角14px, 品牌紫蓝, 发光 shadow
 │                             │
 │   ┌──────┐ ┌──────┐        │
 │   │ ✅通过│ │ ❌驳回│       │  40px, 圆角12px, 等宽均分
 │   └──────┘ └──────┘        │
 │                             │
 │       [⚡ 发起争议]         │  34px, 圆角10px, 小字 warning plain
 └─────────────────────────────┘
```

---

## 4. 移动端适配说明

| 检查项 | 状态 |
|--------|:----:|
| 页面底部安全区 | ✅ `padding-bottom: calc(60px + env(safe-area-inset-bottom, 0px))` |
| 图片框比例 | ✅ `16 / 10` (保持) |
| 子任务按钮高度 | ✅ 34px → 36px (略大) |
| 添加子任务按钮 | ✅ 全宽 44px |
| 操作按钮间隙 | ✅ 8px |
| 横向溢出 | ❌ 无 |

---

## 5. 功能验证

| 检查项 | 结果 |
|--------|:----:|
| 任务详情 PC 端正常 | ✅ 未修改布局结构 |
| 任务详情移动端正常 | ✅ 安全区 + 按钮适中 |
| 主按钮醒目 | ✅ 44px + 发光 shadow + 圆角 14px |
| 次按钮不抢焦点 | ✅ 40px + 无 glow shadow |
| 删除/危险仍有区分 | ✅ `type="danger" plain` 保留 |
| 子任务操作按钮不拥挤 | ✅ 间隙优化 |
| 返回列表正常 | ✅ 未修改返回逻辑 |
| 报名/编辑/删除操作 | ✅ 未修改事件函数 |

---

## 6. Build 结果

| 检查项 | 结果 |
|--------|:----:|
| `npm run build` | ✅ 通过（6.8s） |
| SCSS 编译错误 | ❌ 无 |
| TS 类型错误 | ❌ 无 |
| dist 产物 | ✅ 正常生成 |

---

## 7. git status

```
M apps/frontend/src/views/task/TaskDetail.vue
```

仅 1 个文件修改。

---

## 8. git diff 统计

```
 apps/frontend/src/views/task/TaskDetail.vue | 75 ++++++++++++----------
 1 file changed, 49 insertions(+), 26 deletions(-)
```

---

## 9. 是否建议提交

✅ **建议提交**。

原因：
1. 构建通过
2. 改动精确，仅 1 个文件
3. 所有详情页功能正常
4. PC + 移动端布局正常
5. 按钮层级主次分明
6. git diff 清晰可审计

如需提交，建议 commit message:
```
style(frontend): improve task detail button hierarchy for v0.2.6 Phase 4
```
