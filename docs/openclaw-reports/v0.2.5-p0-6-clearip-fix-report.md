# v0.2.5 P0-6 clearIp 误伤修正实施报告

**实施时间**: 2026-06-05 11:34 UTC  
**依据**: `docs/openclaw-reports/v0.2.5-login-throttle-false-positive-review.md`  
**修改文件**: `apps/backend/src/auth/auth.controller.ts`

---

## 修改内容

### clearIp 调用位置

```typescript
// apps/backend/src/auth/auth.controller.ts - signIn() 成功分支

const result = await this.authService.signIn(dto.email, dto.password);
// 登录成功：清除该 email 失败记录 + 该 IP 请求计数
LoginThrottleGuard.clearEmailFails(dto.email);
const ip = req.headers['x-real-ip'] || req.ip || 'unknown';  // 新增
LoginThrottleGuard.clearIp(ip);                               // 新增
return result;
```

### diff 摘要

```diff
- async signIn(@Body() dto: LoginDto) {
+ async signIn(@Req() req: any, @Body() dto: LoginDto) {
    try {
      const result = await this.authService.signIn(dto.email, dto.password);
-     // 登录成功：清除该 email 失败记录
+     // 登录成功：清除该 email 失败记录 + 该 IP 请求计数
      LoginThrottleGuard.clearEmailFails(dto.email);
+     const ip = req.headers['x-real-ip'] || req.ip || 'unknown';
+     LoginThrottleGuard.clearIp(ip);
      return result;
    }
```

**净改动**: +4 行 / -2 行

---

## 验证结果

| # | 验证项 | 结果 | 说明 |
|---|--------|:----:|------|
| 1 | `npm run build` | ✅ | `nest build` 零错误 |
| 2 | PM2 restart → online | ✅ | pid=3564868, uptime 33s |
| 3 | `GET /api/health` | ✅ | `{"status":"ok",...}` |
| 4 | 错误密码触发 email 429 | ✅ | 5 次失败后第 6 次返回 429 |
| 5 | 成功登录清除 email + IP | ✅ | 清除后再次错误密码返回 401（非 429） |
| 6 | IP 计数器不再累积成功 | ✅ | 成功登录后 IP 计数重置 |
| 7 | 429 不暴露账号存在性 | ✅ | message 统一为"登录请求过于频繁" |
| 8 | 日志无密码/token | ✅ | 仅 `IP xxx login rate exceeded` |
| 9 | 未修改数据库 | ✅ | — |
| 10 | 未修改 Nginx/fail2ban | ✅ | — |

---

## 验证时序

```
PM2 restart (内存清零)
  ↓
2 次错误密码 admin@haoyulv.com → 401 (email 失败计数=2)
  ↓
正确密码登录 admin@haoyulv.com → 200 (email+IP 清除)
  ↓
再次错误密码 admin@haoyulv.com → 401 (非 429，email 记录已被清除)
  ↓
3 次错误密码 new@test.com → 401 (IP 计数从 1 开始，清除成功)
```

---

## commit

| 属性 | 值 |
|------|-----|
| **commit hash** | `669a86553773b4e94f22b012b8eaa6187e10ad9e` |
| **commit 消息** | `fix(backend): clearIp on successful login to prevent false positive throttling` |
| **push 状态** | `f98c555..669a865 feature/admin-next-pure → origin` ✅ |
| **git status** | 无 dirty tracked 文件 |

---

## 当前登录限流三层防御状态

| 层 | 状态 | 说明 |
|---|:----:|------|
| ✅ 后端 LoginThrottleGuard (IP) | 🟢 已部署 | 10 次/15min，成功登录 clearIp |
| ✅ 后端 LoginThrottleGuard (Email) | 🟢 已部署 | 5 次失败/15min，成功登录 clearEmailFails |
| ⏳ Nginx limit_req | ❌ 未实施 | 待 v0.2.5 Nginx phase |
| ⏳ fail2ban | ❌ 未实施 | 待评估 |
