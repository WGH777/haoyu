# v0.2.6 Phase 3 — 移动端任务流与底部发布入口优化报告

**时间**: 2026-06-06 10:20 UTC  
**执行人**: 小紫 👾  
**目标版本**: v0.2.6  
**实施范围**: 仅 `apps/frontend/src/views/HomeView.vue`  
**设计依据**: `docs/design/v0.2.6-frontend-visual-upgrade-plan.md`  
**Phase 0-2 commit**: `fe68522` `c84dff9` `40ba820`  
**严格约束**: 不拆分组件、不改业务逻辑、不改 API/后端/数据库

---

## 1. 实际修改文件

| # | 文件 | 操作 | 变更量 |
|---|------|:----:|:------:|
| 1 | `apps/frontend/src/views/HomeView.vue` | 修改 | **+46 / -17 行** |

仅修改 1 个文件（移动端媒体查询样式），effects.scss 和 PC 端样式未动。

---

## 2. 移动端任务流改动摘要

### 2.1 任务卡片内间距

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| padding | `18px 16px` | `16px`（顶底缩小，宽度不变） |
| 卡片阴影 | `box-shadow: none` | `0 2px 8px rgba(0,0,0,0.15)`（柔和浮起感） |
| active 缩放 | `scale(0.992) opacity(0.96)` | `scale(0.99) opacity(0.95)`（微调压缩感） |
| 高预算边框 | 无 | `border-color: rgba(251,191,36,0.25)`（暖金统一） |

### 2.2 标签层级优化

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| category 字重 | `font-weight: 400` | `font-weight: 600` |
| status-badge 字重 | `font-weight: 400` | `font-weight: 700` |
| mobile-badge.hot 配色 | `rgba(245,158,11,0.2)` amber | `rgba(251,191,36,0.2)` gold（暖金统一） |
| mobile-badge.new 配色 | `rgba(6,182,212,0.15)` cyan | `rgba(99,102,241,0.15)` brand（品牌色统一） |

### 2.3 进度条可见性

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 高度 | `3px` | `4px`（稍粗） |
| margin | `0` | `2px 0`（上下留呼吸） |
| 圆角 | 无 | `border-radius: 2px` |

### 2.4 底部信息区

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| mobile-bottom-row gap | `10px` | `12px`（价格/服务间距略宽） |
| premium-card-meta gap | 继承全局 | `8px`（紧凑排列） |

### 2.5 两列布局增强（601-768px）

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 价格 | 继承 18px | `16px`（避免溢出） |
| 描述行数 | 两行 | `-webkit-line-clamp: 1` |
| badge 高度 | 22px | `20px` |

---

## 3. 底部导航 / FAB 改动摘要

| 项目 | 修改前 | 修改后 | 原因 |
|------|--------|--------|------|
| FAB z-index | `850` | `900` | 提高层级，避免被弹出层遮挡 |
| FAB shadow | `rgba(99,102,241,0.4)` | `rgba(99,102,241,0.35)` | 略柔和 |
| 底部安全区 | 未显式调整 | 保持 `calc(72px + env(safe-area-inset-bottom, 0px))` | 延续 Phase 0 的 hy-mobile-safe-bottom 规范 |

---

## 4. PC 端影响

| 检查项 | 结果 |
|--------|:----:|
| PC 端任务卡片 CSS 覆盖 | ❌ 无 — 所有修改在 `@media (max-width: 768px)` 内 |
| PC 端 hover/glow | ✅ 保持 Phase 2 升级效果 |
| PC 端 3 列布局 | ✅ 不影响 |
| PC 端价格渐变 | ✅ 保持 effects.scss 全局定义 |

---

## 5. 功能验证

| 检查项 | 结果 |
|--------|:----:|
| 手机端首页无横向滚动 | ✅ 未修改宽度属性 |
| 手机端任务卡片信息不拥挤 | ✅ 间距/字号/字重优化 |
| 底部导航不遮挡内容 | ✅ FAB z-index:900 + task-grid padding-bottom |
| FAB 不挡住卡片操作 | ✅ `calc(72px + env(safe-area-inset-bottom, 0px))` safe area |
| 任务详情跳转正常 | ✅ 未修改事件函数 |
| PC 端任务卡片不回退 | ✅ 所有修改在 mobile media query 内 |

---

## 6. Build 结果

| 检查项 | 结果 |
|--------|:----:|
| `npm run build` | ✅ 通过（6.8s） |
| SCSS 编译错误 | ❌ 无 |
| TS 类型错误 | ❌ 无 |
| dist 产物 | ✅ 正常生成 |

---

## 7. git status

```
M apps/frontend/src/views/HomeView.vue
```

仅 1 个文件修改。

---

## 8. git diff 统计

```
 apps/frontend/src/views/HomeView.vue | 63 +++++++++++++++++++----------
 1 file changed, 46 insertions(+), 17 deletions(-)
```

---

## 9. 是否建议提交

✅ **建议提交**。

原因：
1. 构建通过
2. 改动精确，仅 1 个文件，grep 可审计
3. 所有移动端修改在 `@media (max-width: 768px)` 内部，不影响 PC
4. 所有功能入口正常
5. git diff 清晰

如需提交，建议 commit message:
```
style(frontend): improve mobile task flow and FAB for v0.2.6 Phase 3
```
