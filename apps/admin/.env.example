# Admin 前端构建配置 (示例模板)
# 复制本文件为 .env 或 .env.production 并按需修改
# VITE_* 变量在构建时嵌入

# 线上环境平台打包路径
VITE_PUBLIC_PATH = /

# 线上环境路由历史模式
VITE_ROUTER_HISTORY = "h5"

# 是否在打包时使用cdn替换本地库
VITE_CDN = false

# 是否启用gzip/brotli压缩（可选: "gzip" / "brotli" / "none"）
VITE_COMPRESSION = "none"
